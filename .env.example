# Required for the AI Property Advisor (/api/chat) to return real Claude responses.
# Get a key at https://console.anthropic.com/
# Without this set, the chatbot runs in a graceful "demo mode" and says so.
ANTHROPIC_API_KEY=sk-ant-your-key-here
